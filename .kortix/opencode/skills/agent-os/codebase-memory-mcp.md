# Codebase Memory MCP Skill

Persistent memory + navigation layer for large repos.
Agents no longer stuff files into context — they query the memory brain.

**Triggers**: "repo memory", "codebase navigation", "find in repo"
